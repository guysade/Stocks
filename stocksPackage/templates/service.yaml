apiVersion: v1
kind: Service
metadata:
  name: {{ include "stocksPackage.fullname" . }}
  labels:
    {{- include "stocksPackage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stocksPackage.selectorLabels" . | nindent 4 }}
